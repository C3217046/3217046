<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>三次方程式の解を求める</title>
</head>
<body>
    <h1>三次方程式の解を求める</h1>
    <p>三次方程式 \( ax^3 + bx^2 + cx + d = 0 \) の解を以下に示します。</p>
    <button onclick="show()">解を表示</button>
    <p id="output"></p>

    <script>
        function show() {
            // 定数の設定 (a, b, c, d の値を設定)
            const a = 1; // 係数 a
            const b = -6; // 係数 b
            const c = 11; // 係数 c
            const d = -6; // 定数項 d

            // 解を保存する配列とカウント変数
            let anser = [];
            let count = 0;

            // for文で解を探索 (xを1から順に変化させる)
            for (let x = 1; x <= 100; x++) {
                // 三次方程式を計算
                let result = a * Math.pow(x,